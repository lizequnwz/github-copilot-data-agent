from __future__ import annotations

import html
import json
from typing import Any


def render_review_html_document(
    state: dict[str, Any], *, token: str, refresh_html: str
) -> str:
    serialized = json.dumps(state, ensure_ascii=False).replace("</", "<\\/")
    replacements = {
        "__STATE__": serialized,
        "__TOKEN__": json.dumps(token),
        "__TITLE__": html.escape(str(state["model_name"])),
        "__REFRESH__": refresh_html,
    }
    document = _TEMPLATE
    for marker, value in replacements.items():
        document = document.replace(marker, value)
    return document


_TEMPLATE = r'''<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="color-scheme" content="light dark"><title>Review __TITLE__</title>
<style>
:root{--bg:#f5f7fa;--surface:#fff;--surface-2:#eef2f6;--text:#172033;--muted:#526173;--border:#8895a8;--primary:#174ea6;--primary-text:#fff;--success:#14733b;--warning:#8a4b00;--error:#b42318;--ring:#2563eb;--shadow:0 8px 24px rgba(22,34,54,.08)}
*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--bg);color:var(--text);font:16px/1.55 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,select,textarea{font:inherit;color:inherit}button,input,select,textarea,summary{min-height:44px}button{border:1px solid var(--border);border-radius:8px;background:var(--surface);padding:.6rem 1rem;font-weight:700;cursor:pointer;transition:border-color .18s ease,background .18s ease}button:hover{border-color:var(--primary)}button:disabled{cursor:not-allowed;opacity:.5}.primary{background:var(--primary);border-color:var(--primary);color:var(--primary-text)}.danger{color:var(--error);border-color:var(--error)}:focus-visible{outline:3px solid var(--ring);outline-offset:3px}.skip{position:fixed;left:-9999px;top:8px;z-index:99;background:var(--surface);padding:.75rem 1rem}.skip:focus{left:8px}
header{position:sticky;top:0;z-index:20;display:flex;gap:1rem;align-items:center;justify-content:space-between;padding:.75rem clamp(1rem,3vw,2rem);background:var(--surface);border-bottom:1px solid var(--border)}.identity{min-width:0}.eyebrow{color:var(--muted);font-size:.76rem;font-weight:800;letter-spacing:.07em;text-transform:uppercase}h1{font-size:clamp(1.2rem,2.5vw,1.65rem);line-height:1.25;margin:0;overflow-wrap:anywhere}h2{font-size:1.4rem;line-height:1.3;margin:.25rem 0 1rem}h3{font-size:1.05rem;line-height:1.35;margin:0}.header-meta{display:none;gap:.75rem;color:var(--muted);font-variant-numeric:tabular-nums}.actions{display:flex;gap:.5rem;align-items:end}.role-filter{font-size:.78rem;min-width:150px}.shell{width:min(1480px,100%);margin:auto;padding:1rem}nav{display:none}.mobile-nav{width:100%;margin-bottom:1rem}main{min-width:0;padding-bottom:6rem}.panel,.issue,.object-list,.dataset-card,.metric-card{background:var(--surface);border:1px solid var(--border);border-radius:12px;box-shadow:var(--shadow)}.panel{padding:clamp(1rem,3vw,1.5rem);margin-bottom:1rem}.lede{max-width:72ch;color:var(--muted)}.status-line,.card-actions{display:flex;flex-wrap:wrap;gap:.5rem .75rem;align-items:center}.badge{display:inline-flex;align-items:center;gap:.4rem;padding:.2rem .65rem;border:1px solid var(--border);border-radius:999px;font-size:.82rem;font-weight:750}.success{color:var(--success)}.warning{color:var(--warning)}.error{color:var(--error)}.muted{color:var(--muted)}.path{font:12px/1.45 ui-monospace,SFMono-Regular,Menlo,monospace;color:var(--muted);overflow-wrap:anywhere}details{border:1px solid var(--border);border-radius:8px;padding:.35rem .75rem}summary{display:flex;align-items:center;cursor:pointer;font-weight:750}.toolbar{display:grid;gap:.75rem;margin-bottom:1rem}label{display:grid;gap:.3rem;font-weight:700}.helper{color:var(--muted);font-size:.85rem;font-weight:400}input,select,textarea{width:100%;border:1px solid var(--border);border-radius:8px;background:var(--surface);padding:.6rem .75rem}textarea{min-height:108px;resize:vertical}.inline-description{min-height:78px;line-height:1.45}.catalog{display:grid;gap:1rem}.dataset-card{overflow:hidden}.dataset-head{display:grid;gap:.8rem;padding:1rem 1.1rem;background:var(--surface-2);border-bottom:1px solid var(--border)}.dataset-title{display:flex;flex-wrap:wrap;gap:.6rem;align-items:center;justify-content:space-between}.dataset-body{display:grid}.field-row{display:grid;gap:.75rem;padding:1rem 1.1rem;border-bottom:1px solid var(--border)}.field-row:last-child{border:0}.field-meta p,.metric-card p{margin:.25rem 0;color:var(--muted);overflow-wrap:anywhere}.field-description label,.dataset-description label{font-size:.84rem}.metric-grid{display:grid;gap:1rem}.metric-card{display:grid;gap:.75rem;padding:1rem}.metric-expression{padding:.65rem .75rem;background:var(--surface-2);border-radius:8px}.issue-list{display:grid;gap:.75rem}.issue{padding:1rem;border-left:5px solid var(--warning)}.issue.blocking{border-left-color:var(--error)}.issue p{margin:.35rem 0 0;color:var(--muted)}.object-list{overflow:hidden}.object-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:.75rem;align-items:center;padding:.75rem 1rem;border-bottom:1px solid var(--border)}.object-row:last-child{border:0}.object-row p{margin:.2rem 0 0;color:var(--muted);overflow-wrap:anywhere}.empty{padding:1rem;color:var(--muted)}.side-button{width:100%;text-align:left;border:0;border-radius:8px;background:transparent;margin-bottom:.25rem}.side-button[aria-current="page"]{background:var(--surface-2);color:var(--primary)}.result-grid{display:grid;gap:.75rem}.result-grid>div{padding:.75rem;background:var(--surface-2);border-radius:8px}.result-grid dt{font-weight:800}.result-grid dd{margin:0;overflow-wrap:anywhere}.advanced-row{display:grid;gap:.75rem}
dialog.drawer{position:fixed;inset:0 0 0 auto;width:min(720px,100%);height:100dvh;max-height:none;margin:0;border:0;border-left:1px solid var(--border);background:var(--surface);color:var(--text);padding:0;box-shadow:-20px 0 60px rgba(0,0,0,.22)}dialog.drawer::backdrop{background:rgba(15,23,42,.58)}.drawer-shell{height:100%;display:grid;grid-template-rows:auto minmax(0,1fr) auto}.drawer-head,.drawer-foot{display:flex;gap:.75rem;align-items:center;justify-content:space-between;padding:1rem 1.25rem;background:var(--surface);z-index:2}.drawer-head{border-bottom:1px solid var(--border)}.drawer-foot{border-top:1px solid var(--border)}.drawer-body{padding:1.25rem;overflow:auto;display:grid;align-content:start;gap:1rem}.drawer-actions{display:flex;flex-wrap:wrap;gap:.5rem;justify-content:flex-end}fieldset{border:1px solid var(--border);border-radius:10px;padding:1rem;display:grid;gap:.85rem}legend{font-weight:800;padding:0 .3rem}.field-error{color:var(--error);font-size:.85rem}.error-summary{border:2px solid var(--error);border-radius:8px;padding:1rem;background:var(--surface-2)}.error-summary:empty{display:none}.unsaved{font-weight:750;color:var(--warning)}.live{position:fixed;left:-9999px}.toast{position:fixed;right:1rem;bottom:1rem;z-index:60;max-width:min(420px,calc(100% - 2rem));padding:1rem;border-radius:8px;background:var(--text);color:var(--surface);box-shadow:var(--shadow)}.staged-bar{position:fixed;z-index:15;left:1rem;right:1rem;bottom:1rem;display:flex;gap:.75rem;align-items:center;justify-content:space-between;max-width:980px;margin:auto;padding:.75rem 1rem;background:var(--text);color:var(--surface);border-radius:12px;box-shadow:0 12px 35px rgba(0,0,0,.25)}.staged-bar[hidden]{display:none}.staged-bar .bar-actions{display:flex;flex-wrap:wrap;gap:.5rem}.staged-bar button{background:var(--surface);color:var(--text)}.staged-bar .primary{background:var(--primary);color:var(--primary-text)}.queue-summary{display:flex;flex-wrap:wrap;align-items:center;gap:.5rem .75rem;margin-bottom:1rem}.formula-preview{white-space:pre-wrap;margin:0;padding:.8rem;background:#0f172a;color:#e2e8f0;border-radius:8px;font:13px/1.5 ui-monospace,SFMono-Regular,Menlo,monospace}.field-picker{display:grid;gap:.75rem}
@media(max-width:767px){header{display:grid;grid-template-columns:minmax(0,1fr)}.actions{display:grid;grid-template-columns:1fr 1fr}.role-filter{grid-column:1/-1}.drawer-foot{align-items:stretch;flex-direction:column}.drawer-actions{display:grid;grid-template-columns:1fr 1fr}.staged-bar{align-items:stretch;flex-direction:column}.staged-bar .bar-actions{display:grid;grid-template-columns:1fr 1fr}.staged-bar .bar-actions .primary{grid-column:1/-1}}
@media(min-width:768px){.header-meta{display:flex}.shell{display:grid;grid-template-columns:220px minmax(0,1fr);gap:1rem;padding:1.5rem}nav{display:block;position:sticky;top:92px;align-self:start;background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:.75rem}.mobile-nav{display:none}.toolbar{grid-template-columns:minmax(220px,1fr) 220px 220px}.field-row{grid-template-columns:minmax(180px,.8fr) minmax(280px,1.4fr) auto;align-items:start}.metric-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.result-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.field-picker{grid-template-columns:repeat(2,minmax(0,1fr))}}
@media(min-width:1100px){.shell{grid-template-columns:240px minmax(0,1fr);gap:1.5rem;padding:2rem}}
@media(prefers-color-scheme:dark){:root{--bg:#0b1220;--surface:#121d2f;--surface-2:#1b2940;--text:#edf2f8;--muted:#b3c0d2;--border:#71829c;--primary:#8bb8ff;--primary-text:#07111f;--success:#72d69a;--warning:#ffc266;--error:#ff938a;--ring:#9dc4ff;--shadow:none}}
@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important;animation:none!important}}
</style></head><body><a class="skip" href="#workspace">Skip to review workspace</a>
<header><div class="identity"><div class="eyebrow">Semantic review</div><h1>__TITLE__</h1></div><div class="header-meta"><span id="progressText"></span><span id="dirtyState">Draft saved</span></div><div class="actions"><label class="role-filter">Review view<select id="roleFilter"><option value="all">All details</option><option value="business">Business</option><option value="analyst">Analyst</option></select></label><button id="finishButton" type="button">Finish</button><button id="applyButton" class="primary" type="button">Apply and validate</button></div></header>
<div class="shell"><nav aria-label="Review sections" id="sideNav"></nav><main id="workspace" tabindex="-1"><label class="mobile-nav">Review section<select id="sectionSelect"></select></label>__REFRESH__<div id="content"></div></main></div>
<div id="stagedBar" class="staged-bar" hidden><strong id="stagedText"></strong><div class="bar-actions"><button id="undoLast" type="button">Undo last</button><button id="discardDraft" type="button">Discard all</button><button id="saveChanges" class="primary" type="button">Save changes</button></div></div>
<dialog id="editor" class="drawer" aria-labelledby="editorTitle"><div class="drawer-shell"><div class="drawer-head"><div><div class="eyebrow" id="editorType"></div><h2 id="editorTitle">Review semantic object</h2></div><button id="drawerClose" type="button" aria-label="Close editor">Close</button></div><div class="drawer-body"><div id="errorSummary" class="error-summary" tabindex="-1"></div><div id="propertyFields"></div></div><div class="drawer-foot"><button id="removeButton" class="danger" type="button">Remove object</button><div class="drawer-actions"><button id="drawerCancel" type="button">Cancel</button><button id="saveButton" class="primary" type="button">Save to draft</button></div></div></div></dialog>
<div class="live" role="status" aria-live="polite" id="live"></div>
<script>
const INITIAL=__STATE__;const TOKEN=__TOKEN__;
let state=INITIAL,reviewRole=sessionStorage.getItem('semantic-review-role')||'all';
let active=sessionStorage.getItem('semantic-review-section')||'catalog',dirty=false,saveTimer=null,editorObject=null,drawerMode='object',drawerDirty=false,drawerOpener=null,translationChoice=null,metricOperationIndex=null;
const stagedDescriptions=new Map();
const sections=['catalog','metrics','advanced'];
const roleSections={all:sections,business:['catalog','metrics'],analyst:sections};
const labels={catalog:'Catalog',metrics:'Metrics',advanced:'Advanced',datasets:'Tables',fields:'Columns',relationships:'Relationships',ai_context:'AI context'};
function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function announce(v){document.getElementById('live').textContent=v}
function setDirty(v){dirty=v;const el=document.getElementById('dirtyState');el.textContent=v?'Saving draft…':'Draft saved';el.className=v?'unsaved':''}
function operationFor(path){return state.decisions.operations.find(o=>o.path===path)}
function shownValue(prop){const staged=stagedDescriptions.get(prop.path);if(staged)return staged.value;const op=operationFor(prop.path);return op&&op.op!=='remove'?op.value:prop.value}
function propFor(object,label){return object.properties.find(p=>p.label===label)}
function descriptionFor(object){const prop=propFor(object,'Description');return prop?String(shownValue(prop)||''):''}
function expressionFor(value){const dialects=value?.expression?.dialects||[];return dialects.map(d=>`${d.dialect}: ${d.expression}`).join(' · ')}
function objectExpression(object){const prop=propFor(object,'Expression');return prop?expressionFor({expression:shownValue(prop)}):''}
function renderNav(){const visible=roleSections[reviewRole]||sections;if(!visible.includes(active))active=visible[0];const nav=document.getElementById('sideNav'),sel=document.getElementById('sectionSelect');nav.innerHTML=visible.map(s=>`<button type="button" class="side-button" data-section="${s}" aria-current="${active===s?'page':'false'}">${labels[s]}</button>`).join('');sel.innerHTML=visible.map(s=>`<option value="${s}" ${active===s?'selected':''}>${labels[s]}</option>`).join('');nav.querySelectorAll('button').forEach(b=>b.onclick=()=>showSection(b.dataset.section));sel.onchange=()=>showSection(sel.value);const role=document.getElementById('roleFilter');role.value=reviewRole;role.onchange=()=>{reviewRole=role.value;sessionStorage.setItem('semantic-review-role',reviewRole);render()}}
function showSection(section){active=sections.includes(section)?section:'catalog';sessionStorage.setItem('semantic-review-section',active);render()}
function descriptionStats(objects=state.objects.filter(o=>o.section==='datasets'||o.section==='fields')){const values=objects.map(object=>descriptionFor(object).trim());return{total:values.length,complete:values.filter(Boolean).length,missing:values.filter(value=>!value).length}}
function progress(){const stats=descriptionStats();document.getElementById('progressText').textContent=`${stats.complete} of ${stats.total} descriptions complete`;updateChangeBar()}
function updateChangeBar(){const staged=stagedDescriptions.size,committed=state.decisions.operations.length,count=staged+committed,bar=document.getElementById('stagedBar'),save=document.getElementById('saveChanges');bar.hidden=!count;document.getElementById('stagedText').textContent=staged?`${staged} unsaved description ${staged===1?'change':'changes'}${committed?` · ${committed} already in draft`:''}`:`${committed} ${committed===1?'change':'changes'} saved in this draft`;document.getElementById('undoLast').disabled=!count;document.getElementById('discardDraft').disabled=!count;save.hidden=!staged;save.textContent=`Save ${staged} ${staged===1?'change':'changes'}`}
function issuePanel(){const blocking=state.issues.filter(issue=>issue.severity==='blocking'),other=state.issues.filter(issue=>issue.severity!=='blocking');if(!state.issues.length)return '';const cards=blocking.map(issueCard).join(''),details=other.length?`<details><summary>${other.length} additional ${other.length===1?'note':'notes'}</summary><div class="issue-list">${other.map(issueCard).join('')}</div></details>`:'';return `<section class="panel" aria-labelledby="issues-heading"><div class="eyebrow">Needs attention</div><h2 id="issues-heading">${blocking.length?`${blocking.length} blocking ${blocking.length===1?'issue':'issues'}`:'Review notes'}</h2><p class="lede">Issues appear beside the catalog instead of in a separate workflow.</p><div class="issue-list">${cards}${details}</div></section>`}
function issueCard(issue){const target=state.objects.find(o=>String(issue.element||'').endsWith('.'+o.name));return `<article class="issue ${issue.severity==='blocking'?'blocking':''}"><span class="badge ${issue.severity==='blocking'?'error':'warning'}">${esc(issue.severity||'review')}</span><h3>${esc(issue.code||'Review issue')}</h3><p>${esc(issue.message||'')}</p>${target?`<button type="button" data-object="${esc(target.id)}">Review object</button>`:''}</article>`}
function catalog(){const datasets=state.objects.filter(o=>o.section==='datasets'),fields=state.objects.filter(o=>o.section==='fields'),stats=descriptionStats();const cards=datasets.map(dataset=>{const description=propFor(dataset,'Description'),source=propFor(dataset,'Source'),datasetFields=fields.filter(field=>field.context===dataset.name),tableStats=descriptionStats([dataset,...datasetFields]);return `<article class="dataset-card" data-catalog-card data-search="${esc((dataset.name+' '+descriptionFor(dataset)+' '+datasetFields.map(f=>f.name+' '+descriptionFor(f)).join(' ')).toLowerCase())}"><div class="dataset-head"><div class="dataset-title"><div><div class="eyebrow">Table</div><h3>${esc(dataset.name)}</h3><div class="path">${esc(source?shownValue(source):dataset.context)}</div></div><div class="card-actions"><span class="badge ${tableStats.missing?'warning':'success'}">${tableStats.complete}/${tableStats.total} described</span><button type="button" data-object="${esc(dataset.id)}">More table details</button></div></div>${inlineDescription(dataset,description,'Describe the table grain, purpose, scope, and exclusions.')}</div><div class="dataset-body">${datasetFields.map(field=>fieldRow(field)).join('')||'<div class="empty">No columns in this table.</div>'}</div></article>`}).join('');return `${issuePanel()}<section class="panel"><div class="eyebrow">Semantic catalog</div><h2>Tables and columns</h2><p class="lede">Add the business meaning first. Technical expressions remain available only when needed.</p><div class="queue-summary"><span class="badge ${stats.missing?'warning':'success'}">${stats.complete} of ${stats.total} descriptions complete</span><button id="nextMissing" type="button" ${stats.missing?'':'disabled'}>Next missing description</button></div><div class="toolbar"><label>Search catalog<input id="catalogSearch" type="search" placeholder="Table, column, or description"></label><label>Completeness<select id="catalogCompleteness"><option value="all">All descriptions</option><option value="missing">Missing descriptions</option></select></label><label>Changes<select id="catalogChanges"><option value="all">All objects</option><option value="changed">Changed only</option></select></label></div><div class="catalog" id="catalogList">${cards||'<div class="empty">No datasets in this model.</div>'}</div></section>`}
function inlineDescription(object,prop,placeholder){if(!prop)return '';const value=shownValue(prop),changed=!!operationFor(prop.path)||stagedDescriptions.has(prop.path);return `<div class="dataset-description" data-description-wrapper data-missing="${!String(value||'').trim()}" data-changed="${changed}"><label>${object.section==='datasets'?'Table':'Column'} description<span class="helper">The business meaning reviewers and agents will use.</span><textarea class="inline-description" data-inline-description data-object-id="${esc(object.id)}" data-path="${esc(prop.path)}" placeholder="${esc(placeholder)}">${esc(value)}</textarea></label></div>`}
function fieldRow(field){const description=propFor(field,'Description'),value=description?shownValue(description):'',changed=description&&(operationFor(description.path)||stagedDescriptions.has(description.path));return `<div class="field-row" data-field-row data-search="${esc((field.name+' '+value+' '+objectExpression(field)).toLowerCase())}" data-missing="${!String(value||'').trim()}" data-changed="${!!changed}"><div class="field-meta"><div class="eyebrow">Column</div><h3>${esc(field.name)}</h3><p class="path">${esc(objectExpression(field)||'No expression')}</p></div><div class="field-description">${inlineDescription(field,description,'Describe business meaning, valid values, and exclusions.')}</div><button type="button" data-object="${esc(field.id)}">More</button></div>`}
function pendingMetrics(){return state.decisions.operations.map((op,index)=>({op,index})).filter(item=>item.op.op==='add'&&item.op.path==='/semantic_model/0/metrics/-'&&item.op.value&&typeof item.op.value==='object')}
function metrics(){const existing=state.objects.filter(o=>o.section==='metrics').map(o=>({kind:'existing',object:o,value:{name:o.name,description:descriptionFor(o),expression:{dialects:(shownValue(propFor(o,'Expression'))?.dialects||[])}}}));const pending=pendingMetrics().map(item=>({kind:'pending',index:item.index,value:item.op.value}));const cards=[...existing,...pending].map(item=>{const value=item.value;return `<article class="metric-card"><div class="dataset-title"><div><div class="eyebrow">${item.kind==='pending'?'New metric · draft':'Metric'}</div><h3>${esc(value.name)}</h3></div><div class="card-actions">${item.kind==='pending'?`<button type="button" data-edit-pending-metric="${item.index}">Edit</button><button type="button" data-undo="${item.index}">Undo</button>`:`<button type="button" data-object="${esc(item.object.id)}">Review</button>`}</div></div><p>${esc(value.description||'No business description yet.')}</p><div class="metric-expression path">${esc(expressionFor(value)||'No expression')}</div></article>`}).join('');return `<section class="panel"><div class="dataset-title"><div><div class="eyebrow">Shared definitions</div><h2>Metrics</h2></div><button id="addMetric" class="primary" type="button">Add metric</button></div><p class="lede">Start from a common aggregation or write a custom expression. Add a clear business description, validate the expression, and save the draft.</p><div class="metric-grid">${cards||'<div class="empty">No metrics yet. Add the first shared definition.</div>'}</div></section>`}
function objectSection(kind,title,help){const objects=state.objects.filter(o=>o.section===kind);return `<section class="panel"><h2>${title}</h2><p class="lede">${help}</p><div class="object-list">${objects.map(o=>`<div class="object-row"><div><h3>${esc(o.name)}</h3><p>${esc(o.context)}</p></div><button type="button" data-object="${esc(o.id)}">Review</button></div>`).join('')||'<div class="empty">No objects in this section.</div>'}</div></section>`}
function advanced(){const src=state.source||{},promotion=state.promotion||{},pending=state.decisions.operations.map((operation,index)=>`<div class="object-row"><div><strong>${esc(operation.op)} ${esc(operation.path)}</strong></div><button type="button" data-undo="${index}">Undo</button></div>`).join(''),validation=state.review_result?.validation;return `${objectSection('relationships','Relationships','Review join direction and physical key columns only when the model uses joins.')}<section class="panel"><h2>Model status</h2><dl class="result-grid"><div><dt>Source</dt><dd>${esc(src.path||'Unknown')}</dd></div><div><dt>Draft</dt><dd>${esc(state.artifacts.draft)}</dd></div><div><dt>Official OSI</dt><dd>${validation?validation.official_valid?'Valid':'Needs correction':'Not applied yet'}</dd></div><div><dt>Promotion</dt><dd>${promotion.promoted?'Promoted to '+esc(promotion.model_path):state.promotion_enabled?'Validated on Apply':'Promotion disabled'}</dd></div></dl></section>${pending?`<section class="panel"><h2>Draft changes</h2><div class="object-list">${pending}</div></section>`:''}<section class="panel"><h2>Raw operations</h2><p class="lede">Use JSON Pointer only when the guided catalog, metrics, and relationship controls cannot express the change.</p><details><summary>Add, replace, or remove with JSON Pointer</summary><div class="advanced-row"><label>Operation<select id="advancedOp"><option>add</option><option>replace</option><option>remove</option></select></label><label>Path<input id="advancedPath" placeholder="/semantic_model/0/metrics/-"></label><label>JSON value<textarea id="advancedValue" placeholder='{"name":"metric_name"}'></textarea></label><button id="advancedAdd" type="button">Review advanced operation</button></div></details></section>`}
function render(){renderNav();progress();const body=active==='catalog'?catalog():active==='metrics'?metrics():advanced();document.getElementById('content').innerHTML=body;bindRendered();requestAnimationFrame(()=>window.scrollTo(0,Number(sessionStorage.getItem('semantic-review-scroll-'+active)||0)))}
function bindRendered(){document.querySelectorAll('[data-object]').forEach(button=>button.onclick=()=>openObject(button.dataset.object,button));document.querySelectorAll('[data-undo]').forEach(button=>button.onclick=()=>undoOperation(Number(button.dataset.undo)));document.querySelectorAll('[data-inline-description]').forEach(input=>{input.oninput=()=>stageDescription(input);input.onblur=()=>announce('Description ready to save.')});const add=document.getElementById('addMetric');if(add)add.onclick=()=>openMetricBuilder(null,add);document.querySelectorAll('[data-edit-pending-metric]').forEach(button=>button.onclick=()=>openMetricBuilder(Number(button.dataset.editPendingMetric),button));const advancedButton=document.getElementById('advancedAdd');if(advancedButton)advancedButton.onclick=()=>openAdvanced(advancedButton);const next=document.getElementById('nextMissing');if(next)next.onclick=focusNextMissing;const search=document.getElementById('catalogSearch');if(search){search.value=sessionStorage.getItem('semantic-review-catalog-search')||'';const completeness=document.getElementById('catalogCompleteness'),changes=document.getElementById('catalogChanges');completeness.value=sessionStorage.getItem('semantic-review-completeness')||'all';changes.value=sessionStorage.getItem('semantic-review-changes')||'all';search.oninput=()=>{sessionStorage.setItem('semantic-review-catalog-search',search.value);filterCatalog()};completeness.onchange=()=>{sessionStorage.setItem('semantic-review-completeness',completeness.value);filterCatalog()};changes.onchange=()=>{sessionStorage.setItem('semantic-review-changes',changes.value);filterCatalog()};filterCatalog()}}
function filterCatalog(){const query=document.getElementById('catalogSearch').value.toLowerCase(),missing=document.getElementById('catalogCompleteness').value==='missing',changed=document.getElementById('catalogChanges').value==='changed';document.querySelectorAll('[data-catalog-card]').forEach(card=>{const fields=[...card.querySelectorAll('[data-field-row]')];fields.forEach(row=>row.hidden=!row.dataset.search.includes(query)||(missing&&row.dataset.missing!=='true')||(changed&&row.dataset.changed!=='true'));const datasetWrapper=card.querySelector('[data-description-wrapper]'),datasetMatches=card.dataset.search.includes(query)&&(!missing||datasetWrapper?.dataset.missing==='true')&&(!changed||datasetWrapper?.dataset.changed==='true');card.hidden=!datasetMatches&&!fields.some(row=>!row.hidden)})}
function focusNextMissing(){const inputs=[...document.querySelectorAll('[data-inline-description]')].filter(input=>!input.value.trim()&&!input.closest('[hidden]'));if(!inputs.length){showToast('No missing descriptions in the current view.');return}const current=inputs.indexOf(document.activeElement),next=inputs[(current+1)%inputs.length];next.focus();next.scrollIntoView({block:'center'});announce(`Missing description ${((current+1)%inputs.length)+1} of ${inputs.length}.`)}
function stageDescription(input){const object=state.objects.find(o=>o.id===input.dataset.objectId),prop=object?.properties.find(p=>p.path===input.dataset.path);if(!prop)return;const value=input.value.trim(),committed=operationFor(prop.path),base=committed&&committed.op!=='remove'?String(committed.value??''):String(prop.value??'');if(value===base)stagedDescriptions.delete(prop.path);else stagedDescriptions.set(prop.path,{value,prop});updateChangeBar();input.closest('[data-description-wrapper]')?.setAttribute('data-missing',String(!value));input.closest('[data-description-wrapper]')?.setAttribute('data-changed',String(stagedDescriptions.has(prop.path)||!!committed));const row=input.closest('[data-field-row]');if(row){row.dataset.missing=String(!value);row.dataset.changed=String(stagedDescriptions.has(prop.path)||!!committed)}}
function undoOperation(index){state.decisions.operations.splice(index,1);setDirty(true);scheduleDraft();render();announce('Draft change undone.')}
function undoLastChange(){if(stagedDescriptions.size){const path=[...stagedDescriptions.keys()].pop();stagedDescriptions.delete(path);render();announce('Last unsaved description change undone.');return}if(state.decisions.operations.length)undoOperation(state.decisions.operations.length-1)}
function discardAllChanges(){const count=stagedDescriptions.size+state.decisions.operations.length;if(!count||!confirm(`Discard all ${count} draft changes?`))return;stagedDescriptions.clear();state.decisions.operations=[];setDirty(true);scheduleDraft();render();announce('All draft changes discarded.')}
function saveDescriptionChanges(){if(!stagedDescriptions.size)return;const note=auditRecord();for(const item of stagedDescriptions.values())upsert({op:item.prop.exists?'replace':'add',path:item.prop.path,value:item.value,...note});const count=stagedDescriptions.size;stagedDescriptions.clear();setDirty(true);scheduleDraft();render();announce(`${count} description ${count===1?'change':'changes'} saved to the draft.`)}
function propertyInput(prop){const value=shownValue(prop);let control='';if(prop.kind==='text')control=`<input data-value value="${esc(String(value??''))}">`;else if(prop.kind==='select')control=`<select data-value>${(prop.options||[]).map(v=>`<option ${v===value?'selected':''}>${esc(v)}</option>`).join('')}</select>`;else if(prop.kind==='multi_select')control=`<select data-value multiple size="${Math.min(Math.max((prop.options||[]).length,2),8)}">${(prop.options||[]).map(v=>`<option ${(value||[]).includes(v)?'selected':''}>${esc(v)}</option>`).join('')}</select>`;else if(prop.kind==='string_list')control=`<textarea data-value>${esc((value||[]).join('\n'))}</textarea>`;else if(prop.kind==='key_lists'||prop.kind==='key_selects')control=`<textarea data-value placeholder="One key per line; comma-separate composite fields">${esc((value||[]).map(v=>Array.isArray(v)?v.join(', '):v).join('\n'))}</textarea>`;else if(prop.kind==='dimension')control=`<label><input data-value type="checkbox" ${value?.is_time?'checked':''}> Time dimension</label>`;else if(prop.kind==='ai_context'){const v=value||{};control=`<label>Instructions<textarea data-part="instructions">${esc(v.instructions||'')}</textarea></label><label>Synonyms<textarea data-part="synonyms">${esc((v.synonyms||[]).join('\n'))}</textarea></label><label>Example questions<textarea data-part="examples">${esc((v.examples||[]).join('\n'))}</textarea></label>`}else if(prop.kind==='expression'){const dialects=value?.dialects?.length?value.dialects:[{dialect:'SNOWFLAKE',expression:''}];control=dialects.map((d,i)=>`<div class="field-picker"><label>Dialect<input data-dialect="${i}" value="${esc(d.dialect||'')}"></label><label>Expression<textarea data-expression="${i}">${esc(d.expression||'')}</textarea></label></div>`).join('')}return `<div data-property data-path="${esc(prop.path)}"><label>${esc(prop.label)}<span class="helper">${esc(prop.help)}</span></label>${control}<span class="field-error"></span></div>`}
function translationPanel(t){if(!t)return '';return `<fieldset><legend>Translation decision</legend><p><strong>Status:</strong> ${esc(t.status)}</p><p class="path">${esc(typeof t.source_expression==='string'?t.source_expression:JSON.stringify(t.source_expression))}</p><div class="card-actions">${t.can_accept?'<button type="button" data-translation="accepted">Accept translation</button>':''}<button type="button" data-translation="unsupported">Retain as reviewed unsupported</button><button type="button" data-translation="requested">Request evidence</button></div><p class="helper" id="translationState">Choose how the source translation should be treated.</p></fieldset>`}
function openObject(id,opener){editorObject=state.objects.find(o=>o.id===id);if(!editorObject)return;drawerMode='object';translationChoice=null;openDrawer(labels[editorObject.section]||editorObject.section,editorObject.name,opener);document.getElementById('propertyFields').innerHTML=`<fieldset><legend>Semantic values</legend>${editorObject.properties.map(propertyInput).join('')}</fieldset>${translationPanel(editorObject.translation)}`;document.querySelectorAll('[data-translation]').forEach(button=>button.onclick=()=>{translationChoice=button.dataset.translation;document.getElementById('translationState').textContent='Selected: '+button.textContent;drawerDirty=true});document.getElementById('removeButton').hidden=editorObject.protected===true;document.getElementById('saveButton').textContent='Save to draft'}
function openDrawer(type,title,opener){drawerOpener=opener||document.activeElement;drawerDirty=false;document.getElementById('editorType').textContent=type;document.getElementById('editorTitle').textContent=title;document.getElementById('propertyFields').innerHTML='';document.getElementById('removeButton').hidden=true;document.getElementById('saveButton').disabled=false;clearErrors();document.getElementById('editor').showModal();requestAnimationFrame(()=>document.querySelector('#propertyFields input,#propertyFields textarea,#propertyFields select')?.focus()||document.getElementById('saveButton').focus())}
function clearErrors(){document.getElementById('errorSummary').textContent='';document.querySelectorAll('.field-error').forEach(e=>e.textContent='')}
function dismissDrawer(force=false){if(!force&&drawerDirty&&!confirm('Discard unsaved edits in this drawer?'))return false;const dialog=document.getElementById('editor');dialog.close();drawerDirty=false;requestAnimationFrame(()=>drawerOpener?.focus());return true}
function readProperty(node,prop){if(prop.kind==='text'||prop.kind==='select')return node.querySelector('[data-value]').value.trim();if(prop.kind==='multi_select')return[...node.querySelector('[data-value]').selectedOptions].map(o=>o.value);if(prop.kind==='string_list')return node.querySelector('[data-value]').value.split('\n').map(v=>v.trim()).filter(Boolean);if(prop.kind==='key_lists'||prop.kind==='key_selects')return node.querySelector('[data-value]').value.split('\n').map(v=>v.split(',').map(x=>x.trim()).filter(Boolean)).filter(v=>v.length);if(prop.kind==='dimension')return{is_time:node.querySelector('[data-value]').checked};if(prop.kind==='ai_context'){const result={},instructions=node.querySelector('[data-part="instructions"]').value.trim(),synonyms=node.querySelector('[data-part="synonyms"]').value.split('\n').map(v=>v.trim()).filter(Boolean),examples=node.querySelector('[data-part="examples"]').value.split('\n').map(v=>v.trim()).filter(Boolean);if(instructions)result.instructions=instructions;if(synonyms.length)result.synonyms=synonyms;if(examples.length)result.examples=examples;return result}if(prop.kind==='expression')return{dialects:[...node.querySelectorAll('[data-dialect]')].map(input=>({dialect:input.value.trim(),expression:node.querySelector(`[data-expression="${input.dataset.dialect}"]`).value.trim()}))};return null}
function validateProperty(node,prop){const value=readProperty(node,prop);let message='';if(prop.label==='Name'&&!String(value).trim())message='Name is required.';if(prop.kind==='expression'&&value.dialects.some(d=>!d.dialect||!d.expression))message='Every expression needs a dialect and value.';node.querySelector('.field-error').textContent=message;return !message}
function auditRecord(){return{rationale:'Saved through the interactive semantic review workspace.',evidence:[{type:'user',reference:'Interactive semantic review approval'}],confidence:'high',assumptions:[]}}
function showErrors(errors){const summary=document.getElementById('errorSummary');summary.innerHTML=errors.map(error=>`<div>${esc(error)}</div>`).join('');if(errors.length)summary.focus()}
function emptyValue(value){return value===null||value===undefined||value===''||(Array.isArray(value)&&!value.length)||(typeof value==='object'&&!Array.isArray(value)&&!Object.keys(value).length)}
function upsert(op){if(op.op==='add'&&op.path.endsWith('/-')){state.decisions.operations.push(op);return}const index=state.decisions.operations.findIndex(value=>value.path===op.path);if(index>=0)state.decisions.operations[index]=op;else state.decisions.operations.push(op)}
function removePending(path){state.decisions.operations=state.decisions.operations.filter(o=>o.path!==path)}
function saveObject(){const inputs=[...document.querySelectorAll('#propertyFields [data-property]')],invalid=inputs.find(node=>!validateProperty(node,editorObject.properties.find(p=>p.path===node.dataset.path)));if(invalid){showErrors(['Correct the highlighted semantic value.']);invalid.querySelector('input,textarea,select')?.focus();return}const note=auditRecord();let expressionChanged=false;for(const prop of editorObject.properties){const input=inputs.find(node=>node.dataset.path===prop.path),value=readProperty(input,prop);if((!prop.exists&&emptyValue(value))||JSON.stringify(value)===JSON.stringify(prop.value))removePending(prop.path);else{upsert({op:prop.exists?'replace':'add',path:prop.path,value,...note,...(prop.label==='Name'?{intent:'rename'}:{})});if(prop.kind==='expression')expressionChanged=true}}if(editorObject.translation){const choice=translationChoice||(expressionChanged?'accepted':null);if(choice)upsert({op:'replace',path:editorObject.translation.path,value:editorObject.translation[choice+'_value'],...note})}commitAndClose('Changes saved to the review draft.')}
function commitAndClose(message){setDirty(true);scheduleDraft();dismissDrawer(true);render();announce(message)}
function removeObject(){if(!confirm(`Remove ${editorObject.name}? You can undo before Apply.`))return;upsert({op:'remove',path:editorObject.path,...auditRecord()});commitAndClose('Object marked for removal.')}
function metricDatasets(){return state.objects.filter(o=>o.section==='datasets').map(o=>o.name)}
function metricFields(dataset){return state.objects.filter(o=>o.section==='fields'&&o.context===dataset).map(o=>o.name)}
function inferMetricTemplate(expression){let match=expression.match(/^COUNT\(1\)$/i);if(match)return{aggregation:'count_rows'};match=expression.match(/^COUNT\(DISTINCT\s+([A-Za-z_][\w$]*)\.([A-Za-z_][\w$]*)\)$/i);if(match)return{aggregation:'count_distinct',dataset:match[1],field:match[2]};match=expression.match(/^(COUNT|SUM|AVG|MIN|MAX)\(([A-Za-z_][\w$]*)\.([A-Za-z_][\w$]*)\)$/i);return match?{aggregation:match[1].toLowerCase(),dataset:match[2],field:match[3]}:null}
function selected(value,expected){return value===expected?'selected':''}
function openMetricBuilder(operationIndex,opener){drawerMode='metric';metricOperationIndex=operationIndex;const value=operationIndex===null?{}:state.decisions.operations[operationIndex].value||{};openDrawer('Guided metric',operationIndex===null?'Add metric':'Edit new metric',opener);const dialect=value.expression?.dialects?.[0]?.dialect||'SNOWFLAKE',expression=value.expression?.dialects?.[0]?.expression||'',template=inferMetricTemplate(expression),custom=!!expression&&!template,context=value.ai_context||{},datasets=metricDatasets(),chosenDataset=template?.dataset||datasets[0]||'',chosenField=template?.field||metricFields(chosenDataset)[0]||'',aggregation=template?.aggregation||'count_rows';document.getElementById('propertyFields').innerHTML=`<fieldset><legend>Business definition</legend><label>Name<span class="helper">Use letters, numbers, and underscores; start with a letter or underscore.</span><input id="metricName" value="${esc(value.name||'')}"><span class="field-error" id="metricNameError"></span></label><label>Description<span class="helper">Explain what it measures, the population, and important exclusions.</span><textarea id="metricDescription">${esc(value.description||'')}</textarea><span class="field-error" id="metricDescriptionError"></span></label><details ${context.synonyms?.length||context.examples?.length?'open':''}><summary>Optional business context</summary><div class="advanced-row"><label>Synonyms<span class="helper">One per line.</span><textarea id="metricSynonyms">${esc((context.synonyms||[]).join('\n'))}</textarea></label><label>Example questions<span class="helper">One per line.</span><textarea id="metricExamples">${esc((context.examples||[]).join('\n'))}</textarea></label></div></details></fieldset><fieldset><legend>Calculation</legend><div id="templateFields" class="field-picker"><label>Aggregation<select id="metricAggregation"><option value="count_rows" ${selected(aggregation,'count_rows')}>Count rows</option><option value="count" ${selected(aggregation,'count')}>Count values</option><option value="count_distinct" ${selected(aggregation,'count_distinct')}>Count distinct</option><option value="sum" ${selected(aggregation,'sum')}>Sum</option><option value="avg" ${selected(aggregation,'avg')}>Average</option><option value="min" ${selected(aggregation,'min')}>Minimum</option><option value="max" ${selected(aggregation,'max')}>Maximum</option></select></label><label>Table<select id="metricDataset">${datasets.map(name=>`<option ${selected(name,chosenDataset)}>${esc(name)}</option>`).join('')}</select></label><label id="metricFieldLabel">Column<select id="metricField">${metricFields(chosenDataset).map(name=>`<option ${selected(name,chosenField)}>${esc(name)}</option>`).join('')}</select></label></div><details id="metricAdvanced" ${custom||dialect==='ANSI_SQL'?'open':''}><summary>Custom expression and dialect</summary><div class="advanced-row"><label>Logic mode<select id="metricMode"><option value="template" ${custom?'':'selected'}>Use the aggregation above</option><option value="custom" ${custom?'selected':''}>Write a custom expression</option></select></label><div id="customFields"><label>Expression<span class="helper">Use qualified semantic fields, for example SUM(orders.amount).</span><textarea id="metricExpression">${esc(expression)}</textarea></label><button id="insertMetricField" type="button">Insert selected field</button></div><label>Expression dialect<select id="metricDialect"><option ${dialect==='SNOWFLAKE'?'selected':''}>SNOWFLAKE</option><option ${dialect==='ANSI_SQL'?'selected':''}>ANSI_SQL</option></select></label></div></details><label>Expression preview<pre id="metricPreview" class="formula-preview"></pre></label></fieldset>`;document.getElementById('removeButton').hidden=true;document.getElementById('saveButton').textContent='Validate and save';bindMetricBuilder();updateMetricPreview()}
function bindMetricBuilder(){for(const id of ['metricMode','metricAggregation','metricDataset','metricField','metricExpression','metricDialect'])document.getElementById(id).addEventListener('input',updateMetricPreview);document.getElementById('metricDataset').onchange=()=>{const dataset=document.getElementById('metricDataset').value,field=document.getElementById('metricField');field.innerHTML=metricFields(dataset).map(name=>`<option>${esc(name)}</option>`).join('');updateMetricPreview()};document.getElementById('insertMetricField').onclick=()=>{const expression=document.getElementById('metricExpression'),qualified=`${document.getElementById('metricDataset').value}.${document.getElementById('metricField').value}`;expression.setRangeText(qualified,expression.selectionStart,expression.selectionEnd,'end');expression.focus();updateMetricPreview()}}
function generatedMetricExpression(){if(document.getElementById('metricMode').value==='custom')return document.getElementById('metricExpression').value.trim();const aggregation=document.getElementById('metricAggregation').value,dataset=document.getElementById('metricDataset').value,field=document.getElementById('metricField').value,qualified=`${dataset}.${field}`;return aggregation==='count_rows'?'COUNT(1)':aggregation==='count_distinct'?`COUNT(DISTINCT ${qualified})`:`${aggregation.toUpperCase()}(${qualified})`}
function updateMetricPreview(){const custom=document.getElementById('metricMode').value==='custom',aggregation=document.getElementById('metricAggregation').value;document.getElementById('templateFields').hidden=custom;document.getElementById('customFields').hidden=!custom;document.getElementById('metricFieldLabel').hidden=!custom&&aggregation==='count_rows';document.getElementById('metricPreview').textContent=`${document.getElementById('metricDialect').value}: ${generatedMetricExpression()}`}
function metricValue(){const synonyms=document.getElementById('metricSynonyms').value.split('\n').map(v=>v.trim()).filter(Boolean),examples=document.getElementById('metricExamples').value.split('\n').map(v=>v.trim()).filter(Boolean),value={name:document.getElementById('metricName').value.trim(),description:document.getElementById('metricDescription').value.trim(),expression:{dialects:[{dialect:document.getElementById('metricDialect').value,expression:generatedMetricExpression()}]}};if(synonyms.length||examples.length)value.ai_context={...(synonyms.length?{synonyms}:{}),...(examples.length?{examples}:{})};return value}
async function saveMetric(){const value=metricValue(),errors=[];if(!/^[A-Za-z_][A-Za-z0-9_]*$/.test(value.name))errors.push('Metric name must be a safe identifier.');if(!value.description)errors.push('Metric description is required.');if(!value.expression.dialects[0].expression)errors.push('Metric expression is required.');const duplicate=[...state.objects.filter(o=>o.section==='metrics').map(o=>o.name),...pendingMetrics().filter(item=>item.index!==metricOperationIndex).map(item=>item.op.value.name)].some(name=>String(name).toLowerCase()===value.name.toLowerCase());if(duplicate)errors.push('Metric name must be unique.');if(errors.length){showErrors([...new Set(errors)]);return}const candidate={op:'add',path:'/semantic_model/0/metrics/-',value,...auditRecord(),intent:'add_metric'},decisions=JSON.parse(JSON.stringify(state.decisions));if(metricOperationIndex===null)decisions.operations.push(candidate);else decisions.operations[metricOperationIndex]=candidate;const button=document.getElementById('saveButton');button.disabled=true;button.textContent='Validating…';try{if(TOKEN){const preview=await api('/api/preview',{decisions,metric_name:value.name});const official=preview.validation?.official_errors||[];if(official.length){showErrors(official);return}}state.decisions=decisions;commitAndClose('Metric validated and added to the review draft.')}catch(error){showErrors([error.message])}finally{button.disabled=false;button.textContent='Validate and save'}}
function openAdvanced(opener){const op=document.getElementById('advancedOp').value,path=document.getElementById('advancedPath').value.trim();let value;try{if(op!=='remove')value=JSON.parse(document.getElementById('advancedValue').value)}catch(_error){showToast('Advanced value must be valid JSON.',true);return}drawerMode='advanced';editorObject={advancedOp:op,path,value};openDrawer('Advanced operation','Review raw semantic operation',opener);document.getElementById('propertyFields').innerHTML=`<fieldset><legend>Operation</legend><p class="path">${esc(op)} ${esc(path)}</p><pre class="formula-preview">${esc(JSON.stringify(value,null,2)||'remove')}</pre></fieldset>`;document.getElementById('saveButton').textContent='Save operation'}
function saveAdvanced(){if(!editorObject.path.startsWith('/')){showErrors(['Path must be a JSON Pointer beginning with /.']);return}upsert({op:editorObject.advancedOp,path:editorObject.path,...(editorObject.advancedOp==='remove'?{}:{value:editorObject.value}),...auditRecord()});commitAndClose('Advanced operation saved to the review draft.')}
async function saveDrawer(){if(drawerMode==='metric')await saveMetric();else if(drawerMode==='advanced')saveAdvanced();else saveObject()}
function scheduleDraft(){clearTimeout(saveTimer);saveTimer=setTimeout(saveDraft,700)}
async function api(path,payload){const response=await fetch(path,{method:'POST',headers:{'Content-Type':'application/json','X-Review-Token':TOKEN},body:JSON.stringify(payload)}),body=await response.json();if(!response.ok)throw new Error(body.error||'Request failed');return body}
async function saveDraft(){if(!TOKEN){setDirty(true);return}try{await api('/api/draft',{decisions:state.decisions});setDirty(false);announce('Draft saved.')}catch(error){setDirty(true);announce('Draft could not be saved: '+error.message)}}
async function applyAll(){if(stagedDescriptions.size){showToast('Commit or discard staged descriptions before applying.',true);return}if(document.getElementById('editor').open){showToast('Save or close the open drawer before applying.',true);return}if(!TOKEN){downloadDecisions();return}const destination=state.promotion?.model_path||`semantic/models/${String(state.model_name).toLowerCase().replace(/[^a-z0-9]+/g,'_')}.yaml`,confirmation=state.promotion_enabled?`Apply audited decisions and promote to ${destination} if clean?`:'Apply audited decisions and validate without promotion?';if(!confirm(confirmation))return;const button=document.getElementById('applyButton');button.disabled=true;button.textContent='Applying…';try{const body=await api('/api/apply',{decisions:state.decisions,confirm_promote:true});state=body.state;setDirty(false);render();showToast(body.result.promoted?'Model validated and promoted.':'Apply and validation complete.')}catch(error){showToast('Apply failed: '+error.message,true)}finally{button.disabled=false;button.textContent='Apply and validate'}}
function downloadDecisions(){const blob=new Blob([JSON.stringify(state.decisions,null,2)+'\n'],{type:'application/json'}),link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=state.artifacts.draft.split('/').pop();link.click();URL.revokeObjectURL(link.href);announce('Review draft downloaded.')}
async function finish(){if(document.getElementById('editor').open&&!dismissDrawer())return;if(stagedDescriptions.size&&!confirm('Finish without committing staged descriptions?'))return;if(dirty&&!confirm('Finish while the committed draft is still saving?'))return;if(TOKEN){try{await api('/api/finish',{})}catch(error){showToast(error.message,true);return}}showToast('Review session finished. You may close this tab.')}
function showToast(message,isError=false){document.querySelector('.toast')?.remove();const element=document.createElement('div');element.className='toast';element.setAttribute('role',isError?'alert':'status');element.textContent=message;document.body.append(element);setTimeout(()=>element.remove(),5000)}
document.getElementById('drawerClose').onclick=()=>dismissDrawer();document.getElementById('drawerCancel').onclick=()=>dismissDrawer();document.getElementById('saveButton').onclick=saveDrawer;document.getElementById('removeButton').onclick=removeObject;document.getElementById('undoLast').onclick=undoLastChange;document.getElementById('discardDraft').onclick=discardAllChanges;document.getElementById('saveChanges').onclick=saveDescriptionChanges;document.getElementById('applyButton').onclick=applyAll;document.getElementById('finishButton').onclick=finish;document.getElementById('editor').addEventListener('cancel',event=>{event.preventDefault();dismissDrawer()});document.addEventListener('keydown',event=>{const dialog=document.getElementById('editor');if(event.key==='Escape'&&dialog.open){event.preventDefault();event.stopPropagation();dismissDrawer()}if((event.metaKey||event.ctrlKey)&&event.key==='Enter'&&dialog.open){event.preventDefault();saveDrawer()}},true);document.getElementById('editor').addEventListener('input',()=>{drawerDirty=true});if(!TOKEN)document.getElementById('applyButton').textContent='Download draft';let scrollTimer;window.addEventListener('scroll',()=>{clearTimeout(scrollTimer);scrollTimer=setTimeout(()=>sessionStorage.setItem('semantic-review-scroll-'+active,String(scrollY)),120)});window.addEventListener('beforeunload',event=>{if(dirty||stagedDescriptions.size||drawerDirty){event.preventDefault();event.returnValue=''}});render();
</script></body></html>'''
